info:
  name: NewTodo
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/new-todo
  body:
    type: json
    data: |-
      {
        "title": "testwazsxTgreitle",
        "description": "A tagresk abogresdut something",
        "status": {
          "name": "todo"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
